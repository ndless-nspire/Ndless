#!/bin/sh
# Caution, must be kept compatible with dash used by some Linux distros.

NDLESS="$(nspire-tools path)"

# gcc instead of as, to call the C preprocessor
exec nspire-tools gcc -mcpu=arm926ej-s -D GNU_AS "$@" -I "$NDLESS/include"
