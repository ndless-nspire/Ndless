libsyscalls exports symbols which make syscalls (e.g. _show_dialog_box2_) callable from e.g. C programs.
Most of them are generated by mkStubs.php, from the list in include/syscall-list.h.
It also provides a small abstraction layer (stdlib.cpp) to make newlib usable.
